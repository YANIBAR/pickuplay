import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  title: {
    paddingBottom: 5,
  },
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
